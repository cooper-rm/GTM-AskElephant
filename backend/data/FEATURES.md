# Engineered Features (54 total)

Generated by `src/nodes/feature_engineer.py` from the raw deal JSON.

## Deal Features (8)

| Feature | Type | Description |
|---------|------|-------------|
| `amount` | float | Total ACV in dollars |
| `seats` | int | Number of seats purchased |
| `price_per_seat` | float | Monthly price per seat |
| `discount_vs_list` | float | Discount relative to $89 list price (0.0 = full price, 0.365 = 36.5% off) |
| `is_inbound` | binary | 1 if lead source was inbound |
| `product_tier_encoded` | binary | 1 if automations_plus_consulting, 0 if unlimited_automation |
| `has_competitor` | binary | 1 if a competitor was involved in the eval |
| `competitor_{name}` | binary | One-hot: gong, fathom, fireflies, clari, chorus (5 columns) |

## People Features (6)

| Feature | Type | Description |
|---------|------|-------------|
| `num_stakeholders` | int | Total people involved in the deal |
| `has_exec_sponsor` | binary | 1 if an exec sponsor was engaged |
| `has_technical_evaluator` | binary | 1 if a technical evaluator participated |
| `champion_tenure_months` | int | Champion's tenure in their role (months) |
| `unique_roles_count` | int | Number of distinct roles represented |
| `is_single_threaded` | binary | 1 if only 1 person involved (risk signal) |

## Touch Features (13)

| Feature | Type | Description |
|---------|------|-------------|
| `total_touches` | int | Total interactions across the deal |
| `sdr_attempts` | int | Prospecting-stage touches |
| `active_touches` | int | Post-prospecting touches |
| `total_call_minutes` | int | Sum of all call durations |
| `avg_call_duration` | float | Average call length in minutes |
| `n_calls` | int | Number of calls (duration > 0) |
| `n_emails` | int | Number of email/LinkedIn touches |
| `calls_to_emails_ratio` | float | Calls ÷ emails |
| `sales_cycle_days` | int | Days from first touch to close |
| `avg_days_between_touches` | float | Mean gap between consecutive touches |
| `longest_gap_days` | int | Longest gap between any two touches |
| `touch_frequency_trend` | float | Positive = accelerating cadence, negative = slowing |
| `n_reschedules` | int | Number of rescheduled meetings |

## Response Features (4)

| Feature | Type | Description |
|---------|------|-------------|
| `response_rate` | float | Fraction of touches that got a response (0.0–1.0) |
| `avg_response_time_hours` | float | Mean response time across touches |
| `fastest_response_hours` | float | Fastest single response time |
| `sdr_attempts_before_connect` | int | Cold touches before first response |

## Engagement Features (6)

| Feature | Type | Description |
|---------|------|-------------|
| `avg_sentiment_score` | float | Mean sentiment (1.0=positive, 0.5=neutral, 0.25=cautious, 0.0=negative) |
| `sentiment_trend` | float | Second-half avg minus first-half avg (positive = improving) |
| `total_questions_asked` | int | Total Q&A exchanges across all touches |
| `prospect_questions_count` | int | Questions asked by the prospect specifically |
| `total_objections` | int | Total objections raised |
| `unique_objection_themes` | int | Distinct objection topics |

## Company Features (13)

| Feature | Type | Description |
|---------|------|-------------|
| `employee_count` | int | Company headcount |
| `annual_revenue` | int | Company annual revenue |
| `revenue_per_employee` | int | Revenue ÷ employees |
| `industry_{name}` | binary | One-hot: saas_tech, marketing_agency, fintech, proptech, healthcare_it, edtech, ecommerce, managed_it_cybersecurity, professional_services, vertical_saas (10 columns) |
